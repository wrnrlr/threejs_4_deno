/// <reference lib="dom" />
import {
  ShaderMaterial,
  ShaderMaterialParameters,
} from "./ShaderMaterial.d.ts";

export class RawShaderMaterial extends ShaderMaterial {
  constructor(parameters?: ShaderMaterialParameters);
}
